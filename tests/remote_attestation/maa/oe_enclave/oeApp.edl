enclave {
    from "openenclave/edl/logging.edl" import *;
    from "openenclave/edl/syscall.edl" import *;
    from "platform.edl" import *;

   trusted {
        public int setup_tls_server([in, string] const char* port);
        public int run_server([user_check] void* ptr);
    };

    untrusted {
    };
};
